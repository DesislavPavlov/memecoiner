{
  "name": "memecoiner",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "description": "Zero-cost live Solana memecoin observer and paper-strategy comparison lab",
  "scripts": {
    "app": "tsx src/app.ts",
    "start": "npm run app",
    "observer": "tsx src/index.ts",
    "test": "node --import tsx --test src/**/*.test.ts",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test",
    "build": "tsc"
  },
  "dependencies": {
    "@noble/curves": "^1.9.7",
    "bs58": "^6.0.0",
    "dotenv": "^16.4.7",
    "pino": "^9.6.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
